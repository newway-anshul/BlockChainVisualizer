import { AnimateLineDirective } from './animate-line.directive';

describe('AnimateLineDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimateLineDirective();
    expect(directive).toBeTruthy();
  });
});
