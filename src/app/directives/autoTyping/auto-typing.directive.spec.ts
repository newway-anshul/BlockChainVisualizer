import { AutoTypingDirective } from './auto-typing.directive';

describe('AutoTypingDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoTypingDirective();
    expect(directive).toBeTruthy();
  });
});
